version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quote-bot
    restart: always
    networks:
      - application-network

networks:
  application-network:
    external: true
